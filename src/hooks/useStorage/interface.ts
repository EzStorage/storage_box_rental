export enum StorageHandleEvent {
    LocalChange = "localStorage:change:",
    LocalClear = "localStorage:clear",
    SessionChange = "sessionStorage:change:",
    SessionClear = "sessionStorage:clear",
}
